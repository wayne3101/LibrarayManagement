@model IEnumerable<LibraryManegement.Models.Genres>


<div class="container">

    <div>
        <h2 class="float-left">Thể loại</h2>
        <a class="btn btn-outline-primary float-right" href="@Url.Action("Create", "Genres")" role="button">Thêm</a>
    </div>

    <table class=" table table-hover">

        <thead>
            <tr>
                <th>
                    @*@Html.DisplayNameFor(model => model.Name)*@
                    @Html.DisplayName("Danh sách thể loại")
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.ActionLink("Sửa", "Edit", new { id = item.Id }) |
                    @Html.ActionLink("Chi tiết", "Details", new { id = item.Id }) |
                    @Html.ActionLink("Xóa", "Delete", new { id = item.Id })
                </td>
            </tr>
            }
        </tbody>

    </table>
</div>
